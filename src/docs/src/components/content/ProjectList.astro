---
import { PROJECTS } from '../../../config';
import ProjectCard from './ProjectCard.astro';
---

<ul class='list flex gap-16 w-full !mt-6'>
  {
    PROJECTS.map((entry) => (
      <ProjectCard title={entry.Title} link={entry.link} image={entry.image} />
    ))
  }
</ul>
